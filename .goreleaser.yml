version: 2

project_name: jwx

builds:
  - main: ./cmd/jwx
    binary: jwx
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - formats:
      - tar.gz
    name_template: "jwx_{{.Version}}_{{.Os}}_{{.Arch}}"
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

brews:
  - repository:
      owner: manimovassagh
      name: homebrew-tap
    homepage: "https://github.com/manimovassagh/jwx"
    description: "A beautiful CLI for working with JSON Web Tokens"
    license: "MIT"
    install: |
      bin.install "jwx"
    test: |
      system bin/"jwx", "version"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"

snapshot:
  version_template: "{{.Tag}}-next"
